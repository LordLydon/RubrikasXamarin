<?xml version="1.0" encoding="utf-8" ?>
<pages:ConnectedContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:RubrikasMasterDetail.Pages"
             x:Class="RubrikasMasterDetail.Pages.NewCoursePage"
            Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="GUARDAR" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Nombre" FontSize="Medium" />
            <Entry Text="{Binding Name}" Placeholder="Nombre del Curso" FontSize="Small" />
            <Label Text="NRC" FontSize="Medium" />
            <Entry Text="{Binding NRC}" Placeholder="NRC del Curso" FontSize="Small" Keyboard="Numeric" />
        </StackLayout>
    </ContentPage.Content>
</pages:ConnectedContentPage>